{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Nx Serve Backend API",
            "command": "npx nx serve backend-api",
            "cwd": "${workspaceFolder}/",
            "env": {
                "NODE_ENV": "development"
            }
        }
    ]
}